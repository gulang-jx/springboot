<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zl.mybatis.mapper.UserMapper">
	<select id="getAllUser" parameterType="zl.bean.UserBean" resultType="zl.bean.UserBean">
	 	select * from user where name =#{name} order by age desc
	</select>
</mapper>